/** Re-exported from the app's own demo-mode signal generator so tests and
 * the in-app demo mode exercise the exact same synthetic signals. */
export * from "@/lib/demo/syntheticAudio";
export { DEMO_SAMPLE_RATE as TEST_SAMPLE_RATE } from "@/lib/demo/syntheticAudio";
